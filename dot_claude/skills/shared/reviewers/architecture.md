# Architecture Reviewer

Canonical source: `~/.config/dotfiles/agent-review/reviewers/architecture.md`.

This staged Claude file is a migration pointer, not a standalone fallback. When
a review skill asks for this reviewer brief, read the canonical shared file
above and use that content. The repo source for the canonical file is
`dot_config/dotfiles/agent-review/reviewers/architecture.md`.
