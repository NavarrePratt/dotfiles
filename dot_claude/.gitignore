# Runtime state
.claude/
policy-limits.json
sessions/
telemetry/
cache/
paste-cache/
debug/
stats-cache.json
downloads/
file-history/
ide/
plans/
projects/
session-env/
shell-snapshots/
statsig/
todos/
usage-data/
tasks/
teams/
image-cache/

# History (may contain sensitive data)
history.jsonl

# Plugins have their own git repo
plugins/

# Agent memory (may contain sensitive session context)
agent-memory/
agent-memory-local/

# Credentials and secrets
.credentials.json
mcp-servers/.env
mcp-needs-auth-cache.json

# Local overrides (not for sharing)
*.local.json
*.local.md
.claude.json

# Skills symlinked from internal repos (tracked in their source repos)
skills/ask-o11yops

# Migration skill (temporary, local only)
skills/bd-to-br-migration/

backups/

# Issue tracking state
.beads/
.atari/
